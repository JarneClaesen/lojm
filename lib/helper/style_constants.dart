import 'package:flutter/material.dart';

class StyleConstants {
  static const BorderRadius roundedCorner = BorderRadius.all(Radius.circular(8.0));
  static const BorderRadius largeRoundedCorner = BorderRadius.all(Radius.circular(16.0));
}
